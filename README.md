# Match Predictions System 🎯

Система прогнозирования спортивных матчей с аналитикой точности пользователей.

**Стек:** Python 3.11+ | MongoDB 7.0 | Motor (async) | Pydantic v2

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Архитектура](#-архитектура)
- [Установка](#-установка)
- [Запуск](#-запуск)
- [Использование](#-использование)
- [Структура проекта](#-структура-проекта)
- [API моделей](#-api-моделей)
- [Аналитика](#-аналитика)
- [Миграции](#-миграции)
- [Тестирование](#-тестирование)

---

## 🚀 Возможности

- **Регистрация пользователей** с валидацией данных через Pydantic
- **Матчи** — создание, обновление результатов, история
- **Прогнозы** — пользователи делают прогнозы на исходы матчей
- **Аналитика** — расчёт точности прогнозов, рейтинги, статистика
- **Асинхронная работа** с MongoDB через Motor
- **CLI интерфейс** для управления данными
- **Docker-ready** — полная контейнеризация

---

## 🏗 Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                      CLI / Scripts                       │
├─────────────────────────────────────────────────────────┤
│                       Services                           │
│  ┌─────────────┐ ┌─────────────┐ ┌───────────────────┐  │
│  │ UserService │ │MatchService │ │ PredictionService │  │
│  └─────────────┘ └─────────────┘ └───────────────────┘  │
├─────────────────────────────────────────────────────────┤
│                     Repositories                         │
│  ┌────────────────┐ ┌────────────────┐ ┌──────────────┐ │
│  │ UserRepository │ │MatchRepository │ │ PredictionRep│ │
│  └────────────────┘ └────────────────┘ └──────────────┘ │
├─────────────────────────────────────────────────────────┤
│                   Pydantic Models                        │
│  ┌──────┐ ┌───────┐ ┌────────────┐ ┌─────────────────┐  │
│  │ User │ │ Match │ │ Prediction │ │ AnalyticsResult │  │
│  └──────┘ └───────┘ └────────────┘ └─────────────────┘  │
├─────────────────────────────────────────────────────────┤
│                MongoDB (Motor async driver)              │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 Установка

### Требования

- Python 3.11+
- Docker & Docker Compose
- uv (рекомендуется) или pip

### Шаги установки

```bash
# Клонировать репозиторий
cd Mongo-Motor

# Создать виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# или
.venv\Scripts\activate  # Windows

# Установить зависимости
pip install -e ".[dev]"

# Скопировать конфигурацию
cp .env.example .env
```

---

## 🐳 Запуск

### С Docker (рекомендуется)

```bash
# Запустить MongoDB и Mongo Express
cd docker
docker-compose up -d

# Проверить статус
docker-compose ps

# Mongo Express доступен на http://localhost:8081
```

### Локальный запуск приложения

```bash
# Из корня проекта
python main.py

# Или через CLI
predictions --help
```

---

## 💻 Использование

### CLI команды

```bash
# Пользователи
predictions user create --username "john_doe" --email "john@example.com"
predictions user list
predictions user stats <user_id>

# Матчи
predictions match create --home "Team A" --away "Team B" --date "2024-03-15T18:00:00"
predictions match result <match_id> --home-score 2 --away-score 1
predictions match list --status pending

# Прогнозы
predictions predict create --user <user_id> --match <match_id> --home 2 --away 1
predictions predict list --user <user_id>

# Аналитика
predictions analytics user <user_id>
predictions analytics leaderboard --limit 10
predictions analytics accuracy --period month
```

### Программный API

```python
import asyncio
from src.db.connection import get_database
from src.services.user_service import UserService
from src.services.prediction_service import PredictionService

async def main():
    db = await get_database()
    
    # Создать пользователя
    user_service = UserService(db)
    user = await user_service.create_user(
        username="pro_predictor",
        email="pro@example.com"
    )
    
    # Получить аналитику
    prediction_service = PredictionService(db)
    stats = await prediction_service.get_user_accuracy(user.id)
    print(f"Точность: {stats.accuracy_percent}%")

asyncio.run(main())
```

---

## 📁 Структура проекта

```
Mongo-Motor/
├── docker/
│   ├── Dockerfile
│   └── docker-compose.yml
├── src/
│   ├── config/
│   │   ├── __init__.py
│   │   └── settings.py          # Pydantic Settings
│   ├── db/
│   │   ├── __init__.py
│   │   ├── connection.py        # Motor connection
│   │   └── indexes.py           # Index definitions
│   ├── models/
│   │   ├── __init__.py
│   │   ├── base.py              # Base model with ObjectId
│   │   ├── user.py              # User model
│   │   ├── match.py             # Match model
│   │   ├── prediction.py        # Prediction model
│   │   └── analytics.py         # Analytics DTOs
│   ├── repositories/
│   │   ├── __init__.py
│   │   ├── base.py              # Generic repository
│   │   ├── user_repository.py
│   │   ├── match_repository.py
│   │   └── prediction_repository.py
│   ├── services/
│   │   ├── __init__.py
│   │   ├── user_service.py
│   │   ├── match_service.py
│   │   ├── prediction_service.py
│   │   └── analytics_service.py
│   ├── validators/
│   │   ├── __init__.py
│   │   └── custom_types.py      # PyObjectId, custom validators
│   └── cli/
│       ├── __init__.py
│       └── commands.py          # Click CLI
├── data/
│   └── seed/
│       ├── teams.json
│       └── sample_matches.json
├── migrations/
│   ├── __init__.py
│   └── versions/
│       └── 001_initial.py
├── tests/
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_models.py
│   ├── test_repositories.py
│   └── test_services.py
├── scripts/
│   ├── seed_data.py
│   └── run_analytics.py
├── .env.example
├── .gitignore
├── pyproject.toml
├── README.md
└── main.py
```

---

## 📊 API моделей

### User

| Поле | Тип | Описание |
|------|-----|----------|
| id | ObjectId | Уникальный идентификатор |
| username | str | Имя пользователя (уникальное) |
| email | str | Email (уникальный) |
| created_at | datetime | Дата регистрации |
| is_active | bool | Активен ли аккаунт |

### Match

| Поле | Тип | Описание |
|------|-----|----------|
| id | ObjectId | Уникальный идентификатор |
| home_team | str | Домашняя команда |
| away_team | str | Гостевая команда |
| scheduled_at | datetime | Дата и время матча |
| status | MatchStatus | pending / live / finished / cancelled |
| home_score | int? | Голы хозяев |
| away_score | int? | Голы гостей |

### Prediction

| Поле | Тип | Описание |
|------|-----|----------|
| id | ObjectId | Уникальный идентификатор |
| user_id | ObjectId | Ссылка на пользователя |
| match_id | ObjectId | Ссылка на матч |
| predicted_home | int | Прогноз голов хозяев |
| predicted_away | int | Прогноз голов гостей |
| created_at | datetime | Дата прогноза |
| points | int? | Начисленные очки |

---

## 📈 Аналитика

Система начисления очков:

| Результат | Очки | Описание |
|-----------|------|----------|
| Точный счёт | 3 | Угадан точный счёт |
| Исход + разница | 2 | Угадан исход и разница голов |
| Исход | 1 | Угадан только исход (победа/ничья) |
| Мимо | 0 | Прогноз не совпал |

### Метрики пользователя

- **total_predictions** — всего прогнозов
- **correct_outcomes** — угаданных исходов
- **exact_scores** — точных счётов
- **accuracy_percent** — % точности
- **avg_points** — среднее кол-во очков
- **current_streak** — текущая серия
- **best_streak** — лучшая серия

---

## 🔄 Миграции

```bash
# Применить все миграции
predictions migrate up

# Откатить последнюю
predictions migrate down

# Статус миграций
predictions migrate status
```

---

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest

# С покрытием
pytest --cov=src --cov-report=html

# Только unit-тесты
pytest tests/test_models.py -v

# Интеграционные тесты (нужна MongoDB)
pytest tests/test_repositories.py -v
```

---

## 🔧 Переменные окружения

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| MONGO_HOST | localhost | Хост MongoDB |
| MONGO_PORT | 27017 | Порт MongoDB |
| MONGO_DB_NAME | predictions_db | Имя базы данных |
| MONGO_ROOT_USER | admin | Пользователь MongoDB |
| MONGO_ROOT_PASSWORD | secret | Пароль MongoDB |
| LOG_LEVEL | INFO | Уровень логирования |

---

## 📝 Лицензия

MIT License

---

## 🤝 Contributing

1. Fork репозитория
2. Создать feature branch (`git checkout -b feature/amazing`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing`)
5. Открыть Pull Request